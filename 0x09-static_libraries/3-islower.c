#include "main.h"

/**
 * _islower - print 1 if alphabet is lower case or 0 if alphabe is not
 * @c : character to check the case
 * Description: using -s_lower function
 * Return: 0 or 1
 */

int _islower(int c)
{
	return (c > 97 && c < 122);
}
